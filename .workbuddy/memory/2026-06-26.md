# 2026-06-26

## 系统架构审计 + 部署建议书
- 完成 v2.19 系统架构审计：架构 8/10、CI/CD 8/10、算法 6/10、数据质量 3/10
- 核心问题：LS龙头永远0（成分股API超时）、Dashboard用随机数、回测无真实数据
- 建议三阶段路线：Phase I 30天数据积累 → Phase II 回测验证 → Phase III 优化
- 输出 `docs/deployment_plan.html` 详细部署建议书（健康度/路线图/风险清单/禁止事项）

## Dashboard 指标图例
- 添加可展开/收起指标说明（12项指标解释），响应式3/2/1列
- `generate_dashboard.py` 已同步更新，后续云端自动生成带图例

## Phase I 数据管道修复
- **LS龙头修复**: fetch_sector_constituents 加入retry(3次)+exponential backoff + time.sleep限频
- 新增 `_ensure_code_map()` 预缓存板块名称→代码映射，避免重复调_name_em()
- 新增 `_normalize_code()` 统一Sina/EM不同代码格式(SH/SZ前缀, .SH/.SZ后缀)
- match_stocks_to_sectors 移除[:10]截断，处理全部板块；每3个板块pause 1.5s
- 实测: 半导体178stocks, 银行42stocks, 人工智能707stocks, 白酒47stocks ✅
- compute_sector_stats 改用match_stocks_to_sectors(成分股API)，不再依赖无industry字段的Sina数据

## Shadow Mode 基础设施
- 新增 rotation/shadow/ (6文件)
  - signal_snapshot.py — 每日信号快照 json
  - shadow_trade.py — 模拟交易日志(含pipeline链路: Meta→Position→Execution)
  - health_collector.py — API统计+模块状态+缺失率
  - runner.py — 主入口 run_shadow_mode()
- GitHub Actions: midday-report.yml + closing-report.yml 新增 Shadow Mode step
- 时区统一 UTC+8

## Dashboard 数据源标注
- 每张卡片新增 REAL/CACHE/FALLBACK 状态标签
- 当前: 市场/BSI/期货/Meta=REAL, MTF=CACHE, 成本/突破=FALLBACK(⚠️待修复)
- 指标说明图例中新增三条数据源状态解释
